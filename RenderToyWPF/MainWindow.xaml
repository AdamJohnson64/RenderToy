<Window x:Class="RenderToy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RenderToy"
        mc:Ignorable="d"
        Title="RenderToy Experiment" Height="600" Width="1024">
    <UserControl>
        <UserControl.Resources>
            <local:Camera x:Key="Camera"/>
        </UserControl.Resources>
        <Grid>
            <!-- Main Viewport Layer -->
            <local:ViewUser x:Name="ViewMain" Camera="{StaticResource Camera}"/>
            <!-- Wireframe Layer -->
            <local:ViewWire x:Name="ViewWireframe" IsHitTestVisible="False" Camera="{StaticResource Camera}"/>
            <!-- PIP Preview -->
            <Border x:Name="ViewPreview" Background="White" HorizontalAlignment="Right" VerticalAlignment="Top" BorderBrush="Black" BorderThickness="1" CornerRadius="4" Margin="8" Padding="2">
                <local:ViewUser Width="192" Height="128" Camera="{StaticResource Camera}"/>
            </Border>
        </Grid>
    </UserControl>
</Window>
